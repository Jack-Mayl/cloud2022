#   consul服务端口号
server:
  port: 80
spring:
  application:
    name: cloud-consumer-order
  #    consul注册中心地址
  cloud:
    consul:
      #      ip地址
      host: 127.0.0.1
      port: 8500
      discovery:
        service-name: ${spring.application.name}

